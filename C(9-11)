#include <iostream>
#include <cmath>
using namespace std;
long long a[10005];
int main()
{
    long n,s,i;
    cin>>n;
    for(i=0;i<n;i++)
    cin>>a[i];
    s=abs(a[0]);
    for(i=1;i<n;i++)
    s+=abs(a[i]-a[i-1]);    
    cout<<s<<endl;  
    return 0;
}
    
